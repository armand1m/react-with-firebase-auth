language: node_js
sudo: false
cache:
  directories:
    - ~/.npm
node_js:
  - 12 
  - 14
  - 16
install:
  - yarn 
script:
  - yarn lint
  - yarn test
  - codecov -f coverage/*.json
after_success:
  - yarn travis-deploy-once "yarn semantic-release"
  - yarn travis-deploy-once "yarn deploy-example"
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
